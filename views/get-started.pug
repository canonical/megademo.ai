extends layout

block content
  .row.justify-content-center
    .col-lg-8
      .card.md-card.mt-3.mb-5
        .card-body.px-4.py-4
          //- Safe: content is server-side HTML from marked.parse() of a static
          //- committed markdown file (content/get-started.md). Not user input.
          div!= content
